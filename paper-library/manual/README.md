# Manual Inputs

Put any PDF here temporarily if the automatic downloader cannot fetch it.

Recommended flow:

1. Drop the missing PDF in this folder.
2. Rename it to the manifest's `pdf_file` name.
3. Move it to `paper-library/pdfs/`.
4. Mark the item as downloaded in the manifest or download report.

